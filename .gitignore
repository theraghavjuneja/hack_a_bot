chatbot.json
# Anyone seeing this code on github
# chatbot.json is the file containing my 
# important informating
# In the context of Google Cloud, a service account key is a JSON file that represents the identity and credentials of a service account. A service account is a special type of Google account that belongs to your application or a virtual machine (VM), rather than to an individual end user.